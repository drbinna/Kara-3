# Kara on Fly — one always-warm machine, mirroring the Modal setup.
# The turn guards and session state are in-process: keep exactly ONE machine
# (deploy with --ha=false) and never auto-stop it.
app = "kara-3"
primary_region = "iad"

[build]

[env]
  PORT = "8000"
  # Every signed-in user gets the full brain (Obi's call, 2026-07-10).
  # The applicants allowlist still tags trial transcripts for review.
  DEMO_MODE = "0"
  # Work-trial allowlist lives on the volume: editable via fly ssh, no redeploy
  APPLICANTS_FILE = "/data/applicants.txt"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

[[mounts]]
  source = "kara_data"
  destination = "/data"

[[vm]]
  memory = "2gb"
  cpu_kind = "shared"
  cpus = 2
