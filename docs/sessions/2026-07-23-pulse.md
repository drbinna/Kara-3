# Session log — 2026-07-21 → 2026-07-23: demo readiness, beam preservation, Pulse

A record of the working session so it survives context loss. Written 2026-07-23.

> **Update (later same day):** Pulse moved to its own repo,
> https://github.com/drbinna/pulse (local: `~/Downloads/pulse`). The
> `personas/pulse/` paths below no longer exist here; everything lives there now.

## What happened, in order

1. **Demo readiness.** Kara verified end-to-end locally. Warm first-token
   0.77–0.80 s (meets the ≤0.8 s target); cold first request after restart/idle
   runs 1.35–1.8 s, so fire a throwaway request before a live demo. The
   Anthropic key in `.env` had run out of credit (400 "credit balance is too
   low" at fast-brain.js:406); replaced locally and in Fly secrets (app
   `kara-3`) with a user-supplied key. Reminder proven again: a zombie server
   on :8000 survived plain `kill` and needed `kill -9`.

2. **Beam masters preserved.** All beam assets moved from gitignored staging
   into `public/` and pushed (commit 4215808): the a–d 4K still ladder, the
   three 10 s masters, and the canonical `kara-beam-loop.mp4`. The full rebuild
   recipe (prompts, palindrome bake, jsDelivr shipping) is in
   `docs/beam-effect.md` (commit 857e96b).

3. **Beam cloned onto a second character** (user's Seedance "Bane" figure),
   via the user's Higgsfield account, ~53 credits total:
   - Source frame media_id `380f4234-aa48-4a38-8c04-2c8d9e2a2e75`
   - Still pass (nano-banana-pro 4K image-edit, background/lighting only):
     jobs `4b384a0d…` (A) and `5d86e7e4…` (B — chosen)
   - Motion pass (Seedance image-to-video): job `15ba7079…` (720p),
     upscaled to 2K via bytedance: job `cf6468ee…`
   - Palindrome-baked to a 20 s seamless loop; identity checked by frame
     extraction at every stage. Chose still-rebuild over video-restyle because
     the source clip's lighting drifted and restyle risks identity.

4. **Pulse persona created** — a second persona, hosted separately from Kara.
   - `personas/pulse/index.html`: visual sibling of Kara's landing page.
     Gulax font (Velvetyne, SIL OFL — LICENSE.txt ships in `fonts/`) on the
     "Pulse" wordmark only; tagline and lore in system font; left column
     flush-left. Poster is `pulse-beam-loop.mp4` by relative path (17.2 MB —
     under jsDelivr's 20 MB cap, so it can move to the CDN pattern when Pulse
     gets a home). No auth/app script wired yet; Kara's element IDs kept
     offstage for future wiring.
   - `personas/pulse/PERSONA.md`: what Pulse is (fills forms, guides/drives
     navigation on hostile sites), lore (Errol Vance — user's own copy),
     voice rules, and hard guardrails (never submit/pay/sign without spoken
     confirmation; payment/password fields read-only unless dictated;
     profile vault local-only; two misreads → fall back to pointing).

5. **Pulse build brainstorm** (architecture sketch, not yet started):
   Chrome extension content script exposing five tools (read_page,
   fill_fields, highlight, click, scroll_to); reuse Kara's turn-loop
   hardening, Anam client, and Companion/document-PiP pattern for the
   on-screen avatar; profile vault in chrome.storage.local so values never
   transit the server. Milestones: (1) form fill text-only, (2) voice + PiP,
   (3) guided highlight, (4) driven navigation. User's-browser-only first.

## Open items

- Scaffold Pulse extension milestone 1 (offered, not requested yet).
- Real Pulse logo (wordmark is text-only for now) and real hosting.
- Retired Modal deployment may still be running — tear down when ready.
- Root `review/` folder (July 10 page drafts) is untracked and unrelated.
