{
  "name": "anam-claude-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An Anam real-time avatar with a Claude Agent SDK brain (read-only workspace access).",
  "scripts": {
    "start": "node server.js",
    "test:brain": "node test-brain.mjs",
    "postinstall": "playwright install chromium || echo \"(browser download skipped — Cara browsing disabled until: npx playwright install chromium)\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.195",
    "better-sqlite3": "^12.11.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jose": "^6.2.3",
    "playwright": "^1.61.1",
    "zod": "^4.4.3"
  }
}
